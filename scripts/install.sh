#!/usr/bin/env bash
# calibre-web install — shell-bootstrap mirror of the backend's install op.
set -euo pipefail
# TODO: implement install for calibre-web.
echo "[install] calibre-web — not yet implemented"
